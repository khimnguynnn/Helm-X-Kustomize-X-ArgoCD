apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmGlobals:
  chartHome: ../../nginx-app

helmCharts:
  - name: nginx-app
    valuesFile: values-prd.yaml
    releaseName: nginx-prd
    namespace: app-prd

namespace: app-prd

labels:
  - pairs:
    env: prd
